#include "image.hpp"

using namespace std;
using namespace cv;

int main() {
	//string imgName = "Gramado_72k.jpg";
	//Image img_ex(imgName, 0, 0);

	//Mat img = img_ex.loadImage();
	//img_ex.setSize(img.rows, img.cols);
	//pair<int, int> size_ex = img_ex.getSize();

	//cout << size_ex.first << " " << size_ex.second;

	//string newName = "teste.jpg";
	//img_ex.saveImage(img, newName);
	//img_ex.showImage(img);

	//int k = waitKey(0);

	return 0;
}